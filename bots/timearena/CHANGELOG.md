# Changelog — `bots/timearena`

## 0.1.0 (unreleased)

- Rename Python package `timecurve_bot` → `timearena_bot` and CLI `timecurve-bot` → `timearena-bot` (GitLab [#323](https://gitlab.com/PlasticDigits/yieldomega/-/issues/323)).
- Legacy env alias `YIELDOMEGA_TIMECURVE_ADDRESS` and registry JSON key `timecurve` remain supported for address resolution.
