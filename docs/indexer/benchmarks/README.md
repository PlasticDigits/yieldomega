# RPC benchmark artifacts (GitLab [#306](https://gitlab.com/PlasticDigits/yieldomega/-/issues/306))

`bash scripts/benchmark-indexer-rpc-anvil.sh` writes timestamped `rpc-benchmark-*.json` and `rpc-benchmark-*.md` here.

Committed samples are optional; regenerate locally when refreshing baselines. See [`../rpc-load-benchmark.md`](../rpc-load-benchmark.md).
