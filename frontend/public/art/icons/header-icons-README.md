# Header Icons

Generated with `scripts/replicate-art/header_icons_batch.py` using one Replicate create attempt per icon. The script runs all icon jobs in parallel and normalizes each transparent PNG to 256x256.
