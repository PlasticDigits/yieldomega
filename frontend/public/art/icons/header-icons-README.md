# Header Icons

Most PNGs here were generated with `scripts/replicate-art/header_icons_batch.py` using one Replicate create attempt per icon. The script runs all icon jobs in parallel and normalizes each transparent PNG to 256x256.

`header-home.svg` is the current hand-authored YO brand mark used by the dense header and favicon.
