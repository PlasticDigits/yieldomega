# Ecosystem comparison social (May 21)

Generated with `scripts/replicate-art/ecosystem_social_may21_batch.py` — one Replicate create per image, parallel workers, exclusive batch lock, `prediction-ids.json` ledger (no duplicate creates on re-run), typography from gpt-image-2 only (no local overlay). See `prompts.json`.
