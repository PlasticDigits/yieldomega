# Ecosystem comparison social (May 21)

Generated with `scripts/replicate-art/ecosystem_social_may21_batch.py` — one Replicate create per image, parallel workers, typography from gpt-image-2 only (no local overlay). See `prompts.json`.
