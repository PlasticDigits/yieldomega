---
name: yieldomega-guardrails
description: Apply Yieldomega repository guardrails for agent work. Arena v2 — read arena spec, keep logic onchain, AGPL, testing strategy, indexer ingest, wallet modal.
---

# Yieldomega guardrails (Arena v2)

Before substantive changes in this repository:

1. Read `docs/agent-phases.md` for the matching phase, then [`docs/product/time-arena.md`](../../docs/product/time-arena.md) (canonical Arena v2 spec · [#240](https://gitlab.com/PlasticDigits/yieldomega/-/issues/240)) and [`docs/product/arena-v2.md`](../../docs/product/arena-v2.md) for implementation detail. Contributor invariants: [`docs/testing/invariants-and-business-logic.md`](../../docs/testing/invariants-and-business-logic.md) (Arena v2 only — legacy TimeCurve/FeeRouter docs removed [#263](https://gitlab.com/PlasticDigits/yieldomega/-/issues/263)).
2. Respect `LICENSE` and `docs/licensing.md` (AGPL-3.0 default).
3. **Onchain authority:** `TimeArena`, `PodiumVaults`, `AdminSellVault`, `Doubloon`, `ReferralRegistry`. Indexer and frontend are derived only.
4. **Testing:** Follow `docs/testing/strategy.md`; run relevant `forge test`, `cargo test`, and frontend tests when touching each layer.
5. **Indexer:** One SQL transaction per block ([#140](https://gitlab.com/PlasticDigits/yieldomega/-/issues/140)); decode/persist arena `Buy` / `PodiumFunded` / `SeedFunded` / `AdminVaultFunded` when registry includes `TimeArena`. Production env guards ([#142](https://gitlab.com/PlasticDigits/yieldomega/-/issues/142), [#156](https://gitlab.com/PlasticDigits/yieldomega/-/issues/156)).
6. **DOUB buy routing ([#249](https://gitlab.com/PlasticDigits/yieldomega/-/issues/249)):** 40% active + 30% seed + 30% admin per buy — [`ArenaBuyRouting`](../../contracts/src/arena/libraries/ArenaBuyRouting.sol); Forge `ArenaPrizeRouting.t.sol`, `TimeArena.t.sol::test_buy_routes_doub_split`, `AdminSellVault.t.sol`.
6a. **Manual podium top-up ([#261](https://gitlab.com/PlasticDigits/yieldomega/-/issues/261)):** `topUpPodiumPools` — 100% to eight prize vaults (10:7.5 active:seed per category), **no** admin take; **`PodiumPoolsToppedUp`**; indexer [#262](https://gitlab.com/PlasticDigits/yieldomega/-/issues/262).
6b. **XP buy gas ([#265](https://gitlab.com/PlasticDigits/yieldomega/-/issues/265)):** cached `level` + `xpTowardNext`; max 5 level-ups/buy; timer epoch rolls must not clear progression — [`ArenaXp.applyXpGain`](../../contracts/src/arena/libraries/ArenaXp.sol). Base XP/level math ([#250](https://gitlab.com/PlasticDigits/yieldomega/-/issues/250)): [`ArenaXp`](../../contracts/src/arena/libraries/ArenaXp.sol) + [`arenaXpMath.ts`](../../frontend/src/lib/arenaXpMath.ts).
6c. **CRED buy + first-buy bonus ([#268](https://gitlab.com/PlasticDigits/yieldomega/-/issues/268)):** `buyWithCred` burns `charmWad × 100e18 / 1e18`; first `_finishBuy` per wallet schedules **150 CRED** in `epochFixedCredBonus[lastBuyEpoch+1]`; `pendingCred` / `claimCred` include fixed bonus.
6d. **Independent podium timers ([#247](https://gitlab.com/PlasticDigits/yieldomega/-/issues/247)):** `podiumDeadline[4]`, `podiumEpoch[4]`, `rollPodiumEpoch`; Forge `test_podium_*`, `test_roll_podium_*` in `TimeArena.t.sol`; spec [`arena-v2.md`](../../docs/product/arena-v2.md#timers-last-buy--four-podiums).
6f. **Referral CRED ([#253](https://gitlab.com/PlasticDigits/yieldomega/-/issues/253) · flat [#272](https://gitlab.com/PlasticDigits/yieldomega/-/issues/272)):** **`ReferralCredApplied`** — **5 CRED** flat per side (`REFERRAL_CRED_FLAT_WAD`); no CHARM weight bonus; **`buyWithCred`** out of scope; indexer **`idx_arena_referral_cred`**; **`/referrals`** + **`/arena`** CRED UI — [`referrals.md`](../../docs/product/referrals.md), [`INV-REFERRAL-272-FLAT-CRED`](../../docs/testing/invariants-and-business-logic.md#referral-flat-cred-gitlab-272).
6e. **Per-podium timer params ([#271](https://gitlab.com/PlasticDigits/yieldomega/-/issues/271)):** `podiumTimerExtensionSec[4]`, `podiumInitialTimerSec[4]`, caps, hard-reset bands — [`ArenaPodiumTimerConfig`](../../contracts/src/arena/libraries/ArenaPodiumTimerConfig.sol); scoring hooks still use **Last Buy** timer only; Forge `test_start_arena_initial_deadlines_differ_by_category`, `test_multi_podium_deadline_extend`, `test_time_booster_hard_reset_band_240_to_300`.
6e. **WarBow DOUB PvP ([#252](https://gitlab.com/PlasticDigits/yieldomega/-/issues/252)):** steal/guard/revenge/flag; costs `WARBOW_*_DOUB`; BP generation reset on WarBow roll (`warbowBpGeneration`); admin `finalizeWarbowPodium` pays (roll skips auto-pay). Forge `test_warbow_*`, `test_finalize_warbow_podium_pays_after_roll`; play skill [`play-time-arena-warbow`](../../skills/play-time-arena-warbow/SKILL.md).
7. **ERC-20 ingress:** Balance-delta parity on DOUB pulls ([#123](https://gitlab.com/PlasticDigits/yieldomega/-/issues/123)).
8. **DeployDev / DeployProduction:** Arena v2 scripts — [`DeployDev.s.sol`](../../contracts/script/DeployDev.s.sol), [`DeployProduction.s.sol`](../../contracts/script/DeployProduction.s.sol); registry + ops ([#259](https://gitlab.com/PlasticDigits/yieldomega/-/issues/259)).
9. **Frontend env:** `VITE_TIME_ARENA_ADDRESS`, `VITE_PODIUM_VAULTS_ADDRESS`, `VITE_ADMIN_SELL_VAULT_ADDRESS`; primary route **`/arena`** unified page ([#256](https://gitlab.com/PlasticDigits/yieldomega/-/issues/256)) — **`INV-FRONTEND-256-UNIFIED-ARENA`** · [arena-views §256](../../docs/frontend/arena-views.md#unified-arena-page-gitlab-256); indexer **`GET /v1/arena/*`** ([#254](https://gitlab.com/PlasticDigits/yieldomega/-/issues/254)); live podium predictions **`GET /v1/arena/podiums`** from **`idx_arena_podium_live`** ([#273](https://gitlab.com/PlasticDigits/yieldomega/-/issues/273)) — **`INV-INDEXER-PODIUM-PREDICT-LIVE`**.
9a. **Wallet profile modal ([#258](https://gitlab.com/PlasticDigits/yieldomega/-/issues/258)):** participant **`AddressInline`** on live buy rows + podium winners → **`WalletProfileModal`** via **`onOpenProfile`**; stats sections Overview / Podium wins / Spending / XP / WarBow / Referrals / Fun facts; **`GET /v1/arena/wallet/{address}/stats`** ([#255](https://gitlab.com/PlasticDigits/yieldomega/-/issues/255)) — [`arena-views.md`](../../docs/frontend/arena-views.md#wallet-profile-modal-gitlab-258), **`INV-FRONTEND-258-WALLET-PROFILE`**, **`INV-INDEXER-255-WALLET-STATS`**, [`WalletProfileModalSections.test.tsx`](../../frontend/src/components/WalletProfileModalSections.test.tsx).
9c. **CHARM + CRED card ([#257](https://gitlab.com/PlasticDigits/yieldomega/-/issues/257)):** **`ArenaCharmCredCard`** — epoch CHARM, accruing + claimable pending CRED, **`claimCred(lastBuyEpoch - 1)`** — [`arena-views.md`](../../docs/frontend/arena-views.md#charm-cred-card-gitlab-257), **`INV-FRONTEND-257-CHARM-CRED-CARD`**, [`arenaCharmCredClaim.ts`](../../frontend/src/lib/arenaCharmCredClaim.ts).
9b. **CRED buy UI ([#269](https://gitlab.com/PlasticDigits/yieldomega/-/issues/269)):** `buyWithCred` via pay picker; burn from onchain `CRED_PER_CHARM_WAD` — [`arenaCredBurn.ts`](../../frontend/src/lib/arenaCredBurn.ts); optional `VITE_PLAY_CRED_ADDRESS`.
10. **Wallet / chain:** Single-chain wagmi ([#81](https://gitlab.com/PlasticDigits/yieldomega/-/issues/81)); wrong-network write gating ([#95](https://gitlab.com/PlasticDigits/yieldomega/-/issues/95)).
11. **Bots:** [`bots/timearena/`](../../bots/timearena/README.md) — retarget to `TimeArena` proxy; venv + PEP 668 per README.
12. **Retired (do not reintroduce):** Collectible NFT layer ([#241](https://gitlab.com/PlasticDigits/yieldomega/-/issues/241)), Rabbit/Burrow ([#242](https://gitlab.com/PlasticDigits/yieldomega/-/issues/242)), TimeCurve sale-end/redemption/presale ([#243](https://gitlab.com/PlasticDigits/yieldomega/-/issues/243)), FeeRouter five-sink CL8Y ([#244](https://gitlab.com/PlasticDigits/yieldomega/-/issues/244)).

Play skills (participants): [`skills/README.md`](../../skills/README.md) — `play-active-time-arena`, `play-time-arena-doub`, `play-time-arena-warbow`.

Prefer small, reviewable diffs.
